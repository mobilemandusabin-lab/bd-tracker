// To run against a local backend, change API_BASE_URL to
// 'http://localhost:5000/api/v1' before zipping the extension.
const CONFIG = {
  API_BASE_URL: 'https://bd-tracker-81oo.vercel.app/api/v1',
  HEARTBEAT_INTERVAL: 5 * 60 * 1000,
  VERSION_CHECK_INTERVAL: 60 * 60 * 1000
};
